{% extends 'basebackoffice.html.twig' %}

{% block title %}Slider{% endblock %}

{% block body %}
    <h1>Slider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ slider.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slider_index') }}" class="btn btn-outline-primary retourListe">Retour à la liste</a>

    <a href="{{ path('slider_edit', {'id': slider.id}) }}" class="btn btn-outline-success">Editer</a>

    {{ include('backoffice/slider/_delete_form.html.twig') }}
{% endblock %}
