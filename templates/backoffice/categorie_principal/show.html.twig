{% extends 'basebackoffice.html.twig' %}

{% block title %}CategoriePrincipal
{% endblock %}

{% block body %}
	<div class="show-article">
		<h1>CategoriePrincipal</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ categorie_principal.id }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ categorie_principal.description }}</td>
				</tr>
				<tr>
					<th>Code</th>
					<td>{{ categorie_principal.code }}</td>
				</tr>
				<tr>
					<th>Slug</th>
					<td>{{ categorie_principal.slug }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('categorie_principal_index') }}" class="btn btn-outline-primary retourListe">Retour à la liste</a>

		<a href="{{ path('categorie_principal_edit', {'id': categorie_principal.id}) }}" class="btn btn-outline-success">editer</a>

		{{ include('backoffice/categorie_principal/_delete_form.html.twig') }}

	</div>
{% endblock %}
