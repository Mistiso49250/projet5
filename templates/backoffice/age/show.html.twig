{% extends 'basebackoffice.html.twig' %}

{% block title %}Age{% endblock %}

{% block body %}
    <h1>Age</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ age.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ age.code }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ age.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('age_index') }}" class="btn btn-outline-primary">Retour à la liste</a>

    <a href="{{ path('age_edit', {'id': age.id}) }}" class="btn btn-outline-primary">edit</a>

    {{ include('backoffice/age/_delete_form.html.twig') }}
{% endblock %}
