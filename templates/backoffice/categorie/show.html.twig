{% extends 'basebackoffice.html.twig' %}

{% block title %}Categorie
{% endblock %}

{% block body %}
	<div class="show-article">
		<h1>Categorie</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ categorie.id }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ categorie.description }}</td>
				</tr>
				<tr>
					<th>Code</th>
					<td>{{ categorie.code }}</td>
				</tr>
				<tr>
					<th>Slug</th>
					<td>{{ categorie.slug }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('categorie_index') }}" class="btn btn-outline-primary retourListe">Retour à la liste</a>

		<a href="{{ path('categorie_edit', {'id': categorie.id}) }}" class="btn btn-outline-success">editer</a>

		{{ include('backoffice/categorie/_delete_form.html.twig') }}

	</div>
{% endblock %}
