{% extends 'basebackoffice.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ marque.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ marque.image }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ marque.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marque_index') }}" class="btn btn-outline-primary retourListe">Retour à la liste</a>

    <a href="{{ path('marque_edit', {'id': marque.id}) }}" class="btn btn-outline-success">editer</a>

    {{ include('backoffice/marque/_delete_form.html.twig') }}
{% endblock %}
